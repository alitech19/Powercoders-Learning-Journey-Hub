{
  "name": "powerhub-frontend",
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i tailwind.css -o static/css/tailwind.css --minify",
    "watch:css": "npx @tailwindcss/cli -i tailwind.css -o static/css/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
