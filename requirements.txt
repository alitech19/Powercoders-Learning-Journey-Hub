Django>=5.2,<6
psycopg[binary]>=3.1
python-dotenv>=1.0
Pillow>=10.0
bleach>=6.0
markdown>=3.5

celery[redis]>=5.4
django-celery-beat>=2.7
django-redis>=5.4
redis>=5.2
PyYAML>=6.0

# Security — authentication (see docs auth phases)
argon2-cffi>=23.1
django-two-factor-auth>=1.16
django-otp>=1.2
qrcode[pil]>=7.4

# Security — hardening
django-axes>=7.0
django-csp>=3.8

# Observability (auth Phase G)
python-json-logger>=2.0
